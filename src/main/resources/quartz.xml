<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--要调用的工作类-->
    <bean id="job" class="com.baidu.weather.schedule.SpiderJob"/>

    <!--调用对象和方法-->
    <bean id="grabWeather" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject" ref="job"/>
        <property name="targetMethod" value="grabWeather"/>
        <!-- 是否允许任务并发执行。当值为false时，表示必须等到前一个线程处理完毕后才再启一个新的线程 -->
        <property name="concurrent" value="false"/>
    </bean>

    <bean id="grabSuggestion" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject" ref="job"/>
        <property name="targetMethod" value="grabSuggestion"/>
        <!-- 是否允许任务并发执行。当值为false时，表示必须等到前一个线程处理完毕后才再启一个新的线程 -->
        <property name="concurrent" value="false"/>
    </bean>

    <bean id="grabAQI" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject" ref="job"/>
        <property name="targetMethod" value="grabAQI"/>
        <property name="concurrent" value="false"/>
    </bean>

    <bean id="grabFuture" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject" ref="job"/>
        <property name="targetMethod" value="grabFuture"/>
        <property name="concurrent" value="false"/>
    </bean>

    <bean id="grabAlarm" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject" ref="job"/>
        <property name="targetMethod" value="grabAlarm"/>
        <property name="concurrent" value="false"/>
    </bean>


    <bean id="runWeatherTime" class="org.springframework.scheduling.quartz.CronTriggerBean">
        <property name="jobDetail" ref="grabWeather"/>
        <property name="cronExpression" value="0 */12 * * * ?"/>
    </bean>

    <bean id="runSuggestionTime" class="org.springframework.scheduling.quartz.CronTriggerBean">
        <property name="jobDetail" ref="grabSuggestion"/>
        <property name="cronExpression" value="0 20 8,11,18 * * ?"/>
    </bean>

    <bean id="runAQITime" class="org.springframework.scheduling.quartz.CronTriggerBean">
        <property name="jobDetail" ref="grabAQI"/>
        <property name="cronExpression" value="0 */30 * * * ?"/>
    </bean>
    <!--7点多没更新，调到8点-->
    <bean id="runFutureTime" class="org.springframework.scheduling.quartz.CronTriggerBean">
        <property name="jobDetail" ref="grabFuture"/>
        <property name="cronExpression" value="0 0 8,12 * * ?"/>
    </bean>

    <bean id="runAlarmTime" class="org.springframework.scheduling.quartz.CronTriggerBean">
        <property name="jobDetail" ref="grabAlarm"/>
        <property name="cronExpression" value="0 */10 * * * ?"/>
    </bean>


    <!--设置调度 总管理类 如果将lazy-init='false'那么容器启动就会执行调度程序 -->
    <bean id="quartzManager" lazy-init="false" autowire="no"
          class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="triggers">
            <list>
                <!--<ref bean="runWeatherTime"/>-->
                <!--<ref bean="runSuggestionTime"/>-->
                <!--<ref bean="runAQITime"/>-->
                <!--<ref bean="runFutureTime"/>-->
                <ref bean="runAlarmTime"/>
            </list>
        </property>
        <property name="autoStartup" value="true"/>
        <property name="taskExecutor" ref="executor"/>
    </bean>

    <!-- 线程执行器配置，用于任务注册 -->
    <bean id="executor"
          class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <property name="corePoolSize" value="10"/>
        <property name="maxPoolSize" value="15"/>
        <property name="queueCapacity" value="50"/>
    </bean>
</beans>